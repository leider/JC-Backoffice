doctype html
html
  .modal-header
    button.close(type="button", data-dismiss="modal", aria-hidden="true") Schließen&nbsp;&times
    if (!content)
      h4 No Content
    else
      h4 Wiki "#{subdir}"
  .modal-body
    if (content)
      | !{content}
