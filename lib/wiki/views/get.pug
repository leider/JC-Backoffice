extends ../../../views/layout
include ../../../views/formComponents

block title
  | #{pageName} : #{subdir}

block content
  #renameDialog.modal.fade(tabindex='-1')
    .modal-dialog
      .modal-content
        .modal-header
          h3.modal-title #{pageName} Umbenennen
          button.close(type='button', data-dismiss='modal', aria-hidden='true') &times;
        .modal-body
          .row
            .col-12
              h3 Achtung: Verweise
              p Das Umbennen einer Seite ändert nicht die Verweise auf sie.
              p Bitte Suche nach dem Seitennamen und passe die Verweise manuell an.

              form#addonform(action='/wiki/rename/' + subdir + '/' + pageName, method='post')
                +csrf
                fieldset
                  +text('newName', 'Neuer Name', '')
                  button.btn.btn-primary.pull-right(type='submit') Speichern


  .col-12
    .page-header
      h2 #{title}
      .btn-group.pull-right
          a.btn.btn-light(href='/wiki/list/' + subdir + '/', title='Alle Seiten in' + ' "' + subdir + '"'): i.fa.fa-list.fa-fw
          a.btn.btn-light(href='/wiki/versions/' + subdir + '/' + pageName, title='Revisionen'): i.fa.fa-sort-amount-desc.fa-fw
          if (canEdit)
            a.btn.btn-light(data-toggle='modal', href='#renameDialog', title='Umbenennen'): i.fa.fa-cogs.fa-fw
            a.btn.btn-light(href='/wiki/edit/' + subdir + '/' + pageName, title='Bearbeiten'): i.fa.fa-edit.fa-fw
      form.hidden-print.form-search.pull-right(action='/wiki/search', method='post')
        +csrf
        input.form-control(type='text', name='searchtext', placeholder='Wiki durchsuchen')
  .col-12
    | !{content}
